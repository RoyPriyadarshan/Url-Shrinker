<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>URL Shortner</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.0/css/bootstrap.min.css" integrity="sha384-9aIt2nRpC12Uk9gS9baDl411NQApFmC26EwAOH8WgZl5MYYxFfc+NcPb1dKGj7Sk" crossorigin="anonymous"> 
</head>
<body>
    <div class="container">
        <h1 style="text-align: center; font-size: 4em; font-weight: bold; text-decoration: underline;">URL Shortner</h1>
        <br>
    <form action="/shortUrls" method="post" class="form-group form-inline">
        <label for="originalURL" class="col-12 col-md-2" style="font-size: 1.5em; font-style:italic; font-weight: bold;">Original URL </label>
        <input type="url" class="form-control form-control-lg col-12 col-md-7 "  name="originalURL" id="originalURL" placeholder="Enter the URL to Shrink it">
        <button type="submit" class="btn btn-success btn-lg col-12 col-md-2" style="margin-left: auto;" >SHRINK URL</button>
    </form>
<br>
    <div class="table-responsive">
        <table class="table table-striped table-bordered">
            <thead class="table-dark">
                <tr style="font-size: large;">
                    <th>Original URL </th>
                    <th>Shrunk URL</th>
                    <th>Number of Clicks</th>
                </tr>
            </thead>
            <tbody class="table-striped">
                <% shortUrls.forEach( shortUrl => { %> 
                    <tr>
                        <td class="table-responsive"><a href="<%= shortUrl.full %>" ><%= shortUrl.full %> </a></td>
                        <td><a href="<%= shortUrl.short %>"><%= shortUrl.short %></a></td>
                        <td><%= shortUrl.clicks %></td>
                    </tr>
                <% }) %> 
               
            </tbody>
        </table>
    </div>
    </div>
</body>
</html>